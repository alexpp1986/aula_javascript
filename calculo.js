
//opção para calculo de soma e subtracao
function soma (a, b) {
    const result = a + b
    console.log ("Valor da Soma:", result, "\n")
    return result
    }
    const resultado = soma(2, 3);
    

    function sub (x, y) {
        const result = x - y
        console.log ("Valor da sub:", result,)
        return result
      }
      const subtracao = sub(10, 8);

function somaSubtrai(n1, n2) {
let vamossomar = n1 + n2;
let vamosdiminuir = n1 - n2
console.log("somando:", vamossomar);
console.log("subtraido:", vamosdiminuir);
}
somaSubtrai(10 ,8 );
